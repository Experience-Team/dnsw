# Sheet data is fetched from the publicly published CSV URL hardcoded in src/services/sheets.ts.

# Required for Figma API access (src/services/figma.ts).
# Generate a personal access token at: https://www.figma.com/settings → "Personal access tokens"
FIGMA_TOKEN=
